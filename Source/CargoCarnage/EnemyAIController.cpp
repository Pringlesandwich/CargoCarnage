// Fill out your copyright notice in the Description page of Project Settings.

#include "EnemyAIController.h"

/*
void AEnemyAIController::BeginPlay()
{

	FTimerHandle handler;
	GetWorldTimerManager().SetTimer(handler, this, &AEnemyAIController::TrackPlayer, 1.0f, true, 0.0f);

}



void AEnemyAIController::TrackPlayer()
{

	APlayableController * PlayerController = UGameplayStatics::GetPlayerController(GetWorld(), 0);

	ACharacter * PlayableCharacter = PlayerController->GetCharacter();
	if (PlayableCharacter) {
		this->MoveToActor(PlayerController->GetCharacter(), 5);
	}

}
*/
